version: '3.9'
services:
  app:
    build: .
    container_name: doc-qna-app
    command: python main.py
    # volumes:
    #   - .:/app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
